import React from "react";
import { Container } from "reactstrap";

const Footer = () => {
  return (
    <Container
      fluid
      className="p-3 bg-info text-center text-white text-uppercase fixed-bottom"
    >
      LCO GitHub Search app with Firebase
    </Container>
  );
};

export default Footer;
